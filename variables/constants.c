#include <stdio.h>
#define greater(x, y) if(x>y)\
                    printf("%ld is greater than %ld", x,y);\
                    else\
                    printf("%ld is lesser than %ld", x,y);
imain()
{
    greater(9028340, 230480238348);
    return 0 ; 
}
